# Data

This folder contains a small sample dataset: `car_data_sample.csv`.

The training script first looks for `data/car_data.csv`. If it is not found, it tries to download the full dataset from the URL used in the original notebook. If internet is not available, it uses the sample dataset.
